export const metadata = {
  title: 'Conducteur partage - RedacNews',
  description: 'Consultez le conducteur de votre emission',
};

export default function SharedRundownLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return <>{children}</>;
}
